# 实验室设备借用管理系统 - Codex 开发准则

本文件适用于整个仓库。Codex 在分析、修改、测试或提交代码前，必须遵守以下规范。系统级指令、开发者指令和用户的明确要求优先于本文件。

## 1. 开发核心原则

### 1.1 需求优先

- **严禁盲目开发**：不得根据个人猜测随意增加功能。
- **开发前六问**：
  1. **需求**是什么？
  2. **为什么**需要这个需求？
  3. 涉及哪些**业务角色**？
  4. **业务流程**是什么？
  5. **数据**如何变化？
  6. 存在哪些**异常情况**？
- **不确定性处理**：如果需求存在不确定性，应先向用户提问，不得直接假设。

### 1.2 先设计，后编码

- **分析路径**：需求 -> 业务流程 -> 数据模型 -> 接口设计 -> 实现 -> 测试。
- **严禁直接动手**：未完成分析前，不得直接创建 Controller、Service 或 Entity。

### 1.3 最小实现原则

- **聚焦核心**：优先实现当前业务真正需要的功能。
- **拒绝过度设计**：不得为了所谓“扩展性”引入不需要的微服务、消息队列、复杂缓存、权限系统或复杂设计模式。技术方案必须由实际业务需求驱动。

## 2. 设计规范

### 2.1 API 设计原则

- **业务语义**：接口命名应体现清晰的业务含义。
- **命名示例**：

```http
GET    /api/equipment                           // 获取设备列表
GET    /api/equipment/{id}                      // 获取设备详情
POST   /api/borrow-applications                 // 提交借用申请
GET    /api/borrow-applications/{id}            // 查看申请进度
POST   /api/borrow-applications/{id}/approve    // 审批通过
POST   /api/borrow-applications/{id}/reject     // 审批驳回
POST   /api/borrow-records/{id}/return          // 归还设备
```

- **按需设计**：具体 API 必须根据最终需求设计，不得为了凑数而设计无业务意义的接口。

### 2.2 数据库设计原则

- **业务驱动**：设计必须从业务需求出发。
- **质量保证**：
  - 主键设计合理，必要字段设为非空。
  - 唯一性约束与外键关系清晰。
  - 状态字段定义明确，时间字段完整。
- **可追溯性**：借用记录必须保留历史数据，不得因归还操作而物理删除记录。

## 3. 业务健壮性

### 3.1 异常场景处理

设计业务时必须主动考虑并处理以下异常，包括但不限于：

- **设备状态异常**：已借出、维修中、已停用。
- **申请异常**：同一用户重复申请、申请记录不存在。
- **归还异常**：重复归还、非本人操作、借用记录不存在。
- **管理异常**：管理员处理不存在的申请。
- **并发异常**：多人同时抢借同一设备。
- **资产异常**：设备损坏、设备丢失、借用超期。
- **反馈规范**：异常处理必须符合业务规则，不得统一返回含义模糊的“操作失败”。

## 4. Codex 工作行为规范

### 4.1 工作方式（强制流程）

Codex 在采取任何行动或修改代码前，必须严格执行以下流程：

1. **深度分析需求**：阅读并理解 `REQUIREMENTS_V1.md` 及相关文档，确保对业务逻辑有完整认知。
2. **技术选型决策**：在引入任何新技术、框架或工具前，必须分析多种备选方案，对比优缺点，并交由用户决策最终选型。
3. **模块设计决策**：在编写具体功能模块前，必须分析多种实现场景，包括正常流程与各种边缘/异常情况，并交由用户决策采用哪种实现方案。
4. **环境检查**：阅读 `AGENTS.md` 和 `README.md`，理解现有架构。
5. **明确变更范围**：给出详细的实现方案并明确需要修改的文件列表。
6. **严禁盲目操作**：不得在未经过上述分析与决策流程的情况下大规模修改代码。

### 4.2 模块范围与读取纪律

- **按指定模块开发**：严格围绕用户指定的模块和任务范围工作。没有必要的情况下，不得随意翻看其余模块的代码。
- **禁止无脑扫描**：不得无脑扫描整个项目。仅在定位依赖、确认接口契约或验证跨模块影响确有必要时，进行最小范围搜索，并说明原因。
- **禁止重复读取**：不得反复读取已经读取过且内容未变化的文件。需要引用时优先使用已获得的信息。
- **只改相关代码**：只修改任务相关代码，不得顺手重构、格式化或改动无关代码。
- **精准验证优先**：优先运行与变更直接相关的单元测试、模块测试或单接口验证，不得每次都运行整个项目。

### 4.3 代码修改原则

- **复用优先**：优先复用已有代码，保持风格一致。
- **兼容性**：不得随意改变已有接口、数据库结构或引入新依赖。
- **专注任务**：不得删除现有功能，不得修改与当前任务无关的代码。
- **改进流程**：发现现有设计存在问题时，遵循：**说明问题 -> 给出方案 -> 用户确认 -> 开始修改**。

### 4.4 Git 提交规范

- **原子提交**：每次提交应只包含一个逻辑上的功能点或修复。
- **提交信息语义化**：
  - `feat: [业务描述]`：新功能开发。
  - `fix: [业务描述]`：修复 Bug。
  - `docs: [业务描述]`：文档更新。
  - `refactor: [业务描述]`：代码重构。
  - `style: [业务描述]`：格式调整，不影响业务逻辑。
- **严禁无意义提交**：不得使用 "update"、"fix" 等含义模糊的提交信息。

## 5. 测试与交付

### 5.1 测试要求

功能完成后必须验证以下流程：

- **正常流程**：创建设备 -> 查看设备 -> 提交借用 -> 借用成功 -> 归还 -> 状态恢复。
- **异常流程**：验证“设备已借出后再次借用”应失败。
- **并发流程**：验证“多人同时申请同一设备”最终只能成功一个。
- **一致性校验**：测试重点是验证 **业务状态 + 数据库状态 + 借用记录** 的三方一致性。

## 6. 责任划分（重要）

| 责任人 | 负责内容 |
| :--- | :--- |
| **人（User）** | 需求理解、产品决策、业务规则定义、架构方向、技术选型、最终代码审核 |
| **Codex** | 阅读代码、分析方案、编写/修改代码、编写测试、辅助 Debug、解释实现细节 |

**核心底线：Codex 不应在没有明确需求的情况下自行改变核心业务规则。**
