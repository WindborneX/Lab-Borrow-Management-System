# 实验室设备借用管理系统 - V1 需求深度分析报告

根据 [AGENT_INSTRUCTIONS.md](file:///D:/Projects/Lab Equipment Borrowing Management System/AGENT_INSTRUCTIONS.md) 准则 1.1，对实验室设备借用管理系统 V1 阶段进行深度分析。

## 1. 需求是什么？
实现一个最小可行性产品（MVP），用于数字化记录和管理实验室设备的借还流程。
*   **设备展示**：实时显示设备及其当前状态。
*   **借用流程**：借用人在线登记借用，系统自动锁定设备。
*   **归还流程**：借用人在线登记归还，系统释放设备状态。
*   **操作记录**：保存完整的借还历史，供追溯使用。

## 2. 为什么需要？
*   **解决状态不明**：Excel 无法实时共享和自动更新，导致成员不知道设备是否可用。
*   **防止借用冲突**：QQ 群口头告知容易被淹没，系统需要具备“排他性借用”逻辑。
*   **补全记录缺失**：通过系统强制关联用户 ID，确保每一笔借用都有迹可循。
*   **及时更新状态**：将物理操作与数字化记录解耦，简化更新流程。

## 3. 涉及哪些业务角色？
*   **借用人 (Borrower)**：实验室成员（学生）。主要操作：查询设备、提交借用申请、归还设备。
*   **管理员 (Administrator)**：实验室设备负责人。主要操作：录入/删除设备、维护设备状态、查看全局借用台账、处理异常借用（如强行归还）。

## 4. 业务流程 (Business Process)
1.  **资产准备**：管理员在后台录入设备名称、分类及标签。设备初始状态为 `AVAILABLE`。
2.  **查询与锁定**：借用人查看列表，选择 `AVAILABLE` 设备，点击“借用”。系统校验成功后，设备状态变为 `BORROWED`，生成一条 `ACTIVE` 借用记录。
3.  **使用阶段**：借用人持有物理设备。
4.  **归还与释放**：借用人点击“归还”。系统更新借用记录为 `RETURNED`，设备状态恢复为 `AVAILABLE`。

## 5. 数据如何变化？
| 操作 | 设备状态变化 (Equipment.status) | 记录表变化 (BorrowRecord) |
| :--- | :--- | :--- |
| 管理员录入 | -> `AVAILABLE` | 无 |
| 成员借用 | `AVAILABLE` -> `BORROWED` | 新增记录 (状态: `ACTIVE`, 借用时间: Now) |
| 成员归还 | `BORROWED` -> `AVAILABLE` | 更新记录 (状态: `COMPLETED`, 归还时间: Now) |
| 设备维修 | `AVAILABLE`/`BORROWED` -> `MAINTAINING` | 若借用中，需处理未完成记录 |

## 6. 异常情况 (Exceptions)
*   **并发冲突**：两人同时借用同一设备（数据库行锁解决）。
*   **非法操作**：
    *   借用已借出/维修中的设备（校验状态）。
    *   非本人归还他人的借用记录（校验用户 ID）。
*   **数据完整性**：
    *   归还已归还的设备（重复操作拦截）。
    *   借用不存在的设备（ID 校验）。
*   **资产异常**：借用超期、设备损坏（在 V1 中先通过手动备注处理）。
